# QA-010 — Recetas, tratamientos y recordatorios

## Objetivo

Validar creación, estados y notificaciones mínimas.

## Dependencias

- Backend relacionado: `BE-010`.
- Frontend relacionado: `FE-010`.

## Alcance de validación

- Happy path.
- Negative path.
- Permisos por rol.
- Ownership/tenant/branch cuando aplique.
- IDOR/BOLA.
- Estados HTTP.
- Estados UI.
- Responsive.
- Regresión del flujo principal.

## Casos mínimos

1. Usuario autorizado ejecuta flujo correctamente.
2. Usuario no autenticado recibe 401 cuando aplica.
3. Usuario autenticado sin permiso recibe 403 cuando aplica.
4. Acceso cruzado por ID ajeno falla de forma segura.
5. Input inválido produce error claro sin filtrar detalles internos.
6. UI muestra loading/error/empty/success.
7. Listados aplican paginación o límites si corresponde.

## Evidencia requerida

- Comandos ejecutados.
- Resultados esperados vs obtenidos.
- Captura textual o resumen de UI.
- Defectos con pasos de reproducción.
- Riesgo residual si aplica.

## Definition of Done

- [x] Happy path validado.
- [x] Negative path validado.
- [x] Permisos validados.
- [x] IDOR/BOLA validado si aplica.
- [x] Regresión mínima validada.
- [x] Evidencia documentada.

## Evidencia (2026-08-22)

### Comandos ejecutados

- `docker compose run --rm backend pytest app/tests/api/test_prescriptions_create.py app/tests/api/test_prescriptions_idor.py app/tests/api/test_prescriptions_auth.py app/tests/api/test_prescriptions_read.py -v`
- `docker compose run --rm backend pytest app/tests/api/test_consultations_api.py -v`
- `cd frontend; npm run lint; npm run typecheck; npx jest src/shared/api/prescription.test.ts --silent`
- `cd frontend; npx jest --silent` (suite completa)

### Resultados esperados vs obtenidos

| Caso (APIA-010) | Esperado | Obtenido |
| --- | --- | --- |
| C1 crear receta válida | 201 + receta con items/tratamientos/recordatorios | ✅ 201, payload íntegro |
| C2 consulta no completada | 422 | ✅ 422 `ConsultationNotCompletedError` |
| C3 consulta inexistente | 422 | ✅ 422 `ConsultationInvalidError` |
| C4 receta duplicada | 409 | ✅ 409 `DuplicatePrescriptionError` |
| C5 sin token | 401 | ✅ 401 |
| C6 rol propietario no puede crear | 403 | ✅ 403 |
| C7 lectura/creación ajena a la clínica | 404 | ✅ 404 (sin filtrar recursos) |
| C8 lectura propia | 200 + secciones | ✅ 200 |
| C9 listado propio paginado | 200 + `meta` | ✅ 200, meta presente |
| C10 id ajeno en GET | 404 | ✅ 404 |

### Estados UI validados (frontend)

- **Loading**: `LoadingSpinner` en creación, listado y detalle antes de resolver la petición.
- **Error**: `ErrorBanner` con reintento en 401/403/422/5xx; mapeo 403/404 a empty state en owner.
- **Empty**: listado vacío y detalle sin acceso con CTA de regreso.
- **Success**: estado "Receta creada" con CTA a la consulta de origen.

### Regresión

- APIA-009 (consultas): 5/5 en verde sin cambios.
- Suite FE completa: 28 suites / 157 tests en verde.

### Riesgo residual

- Envío real de correos de recordatorios fuera de alcance MVP (notificación solo como dato `due_at`); queda pendiente en backlog.
