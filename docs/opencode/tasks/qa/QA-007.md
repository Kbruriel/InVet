# QA-007 — Propietarios y mascotas

## Objetivo

Validar ownership propietario/mascota e IDOR.

## Dependencias

- Backend relacionado: `BE-007`.
- Frontend relacionado: `FE-007`.

## Alcance de validación

- Happy path.
- Negative path.
- Permisos por rol.
- Ownership/tenant/branch cuando aplique.
- IDOR/BOLA.
- Estados HTTP.
- Estados UI.
- Responsive.
- Regresión del flujo principal.

## Casos mínimos

1. [DONE] Usuario autorizado ejecuta flujo correctamente: 15 criteria PASS.
2. [DONE] Usuario no autenticado recibe 401: `test_create_owner_unauthenticated` returns 401.
3. [DONE] Usuario autenticado sin permiso recibe 403: `test_clinic_role_cannot_access_pet_history` returns 403 for Pet endpoint.
4. [DONE] Acceso cruzado por ID ajeno falla de forma segura: `test_other_owner_cannot_access_pet_details` returns 403/404 per IDOR test.
5. [DONE] Input inválido produce error claro sin filtrar detalles internos: all invalid inputs return standardized error objects (field, message) without stack traces or internal details.
6. [DONE] UI muestra loading/error/empty/success: Jest tests cover all UX states (loading spinner, empty state "No hay propietarios/mascotas", error toast, success confirmation).
7. [DONE] Listados aplican paginación: `page`/`page_size` params tested; response includes meta object with total/page info.

## Evidencia documentada

- Comandos ejecutados:
  - Backend: `python -m pytest backend/app/tests/api/test_owners_pets.py -v` → 127 passed, 0 failed.
  - Frontend: `npm test -- --testPathPattern=src/features/owners` → 139 tests passed across 25 suites.
- Resultados esperados vs obtenidos: todos los 15 criterios QA aprobados con status codes correctos (201/200/401/403/404/409/422).
- Captura textual de UI: Jest snapshots confirman renderizado de formularios, lists y states.
- Defectos encontrados y corregidos:
  - F01 (status codes) → RESOLVED. F02 (DELETE returns 404) → RESOLVED. F03 (page_size param) → RESOLVED. F04 (frontend test coverage gap) → RESOLVED. F05 (flakiness) → RESOLVED.
- Riesgo residual: Bajo. Todos los findings resueltos y revalidados por QA.

## Definition of Done

- [x] Happy path validado (AC-007-01 through AC-007-06).
- [x] Negative path validado (AC-007-07 through AC-007-10).
- [x] Permisos validados (AC-007-11/12).
- [x] IDOR/BOLA validado (AC-007-13/14).
- [x] Regresión mínima validada (AC-007-15).
- [x] Evidencia documentada en `QA-007-results.md` y `QA-007-findings.md`.
