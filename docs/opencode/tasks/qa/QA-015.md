# QA-015 — Reportes operativos básicos

## Objetivo

Validar datos agregados, permisos y paginación.

## Dependencias

- Backend relacionado: `BE-015`.
- Frontend relacionado: `FE-015`.

## Alcance de validación

- Happy path.
- Negative path.
- Permisos por rol.
- Ownership/tenant/branch cuando aplique.
- IDOR/BOLA.
- Estados HTTP.
- Estados UI.
- Responsive.
- Regresión del flujo principal.

## Casos mínimos

1. Usuario autorizado ejecuta flujo correctamente.
2. Usuario no autenticado recibe 401 cuando aplica.
3. Usuario autenticado sin permiso recibe 403 cuando aplica.
4. Acceso cruzado por ID ajeno falla de forma segura.
5. Input inválido produce error claro sin filtrar detalles internos.
6. UI muestra loading/error/empty/success.
7. Listados aplican paginación o límites si corresponde.

## Evidencia requerida

- Comandos ejecutados.
- Resultados esperados vs obtenidos.
- Captura textual o resumen de UI.
- Defectos con pasos de reproducción.
- Riesgo residual si aplica.

## Definition of Done

- [x] Happy path validado. — `qa015_verify.py` T01: 8/8 checks; appointments=12, services=18, pets active=3, consultations=3, ratings clinic_avg=4.11 n_vet=1, payments total=202 total_amount (QA-015-results.md).
- [x] Negative path validado. — `test_reports_invalid_input.py`: 18 period 422 (6 endpoints) + 12 pagination 422 (4 paged) + 1 valid forwarded; fechas inválidas → 422 con mensaje legible (APIA-C7); 30 errores ruff → 0.
- [x] Permisos validados. — `test_reports_auth.py`: 401 sin token / token inválido / token expirado en los 6 endpoints (AC-015-09).
- [x] IDOR/BOLA validado si aplica. — `test_reports_tenant_isolation.py`: tenant A/B no se filtran cruzado; `clinic_id` derivada exclusivamente del JWT (no query param) → BOLA-safe (QA-015-T03, QF-015-02 `pass: true`).
- [x] Regresión mínima validada. — `pytest backend/app/tests -q` → **630 passed, 1 skipped**; `pytest backend/app/tests/usecases/test_reports_*.py` → 56 passed (QF-015-01 determinism regression suite).
- [x] Evidencia documentada. — `QA-015-results.md` (Decision `APPROVED`, 129/129 BE-015), `QA-015-findings.md` (QF-015-01/02 `RESOLVED`, QF-015-03 `ACCEPTED_RISK`), `qa_scripts/qa015_verify.py` + `qa_scripts/qa015_results.json`, `BE-015-checks.md` (Decision `APPROVED`).

## Evidencia de cierre (2026-09-08)

- Decision global: **`APPROVED`** (`QA-015-results.md`); estado findings: **`RESOLVED`** (QF-015-03 `ACCEPTED_RISK` justificado y fuera de scope).
- `python backend/scripts/validate_slice_plan.py BE-015 --stage qa` → **`PASS`**.
- `python backend/scripts/validate_slice_plan.py BE-015 --stage secure-persistence` → **`PASS`**.
- 13 fallos full-suite `ACCEPTED_RISK` (QF-015-03) son de infraestructura de agencia/planning (schema v3, catálogos), ajenos a reportes, auth, persistencia ni ownership de BE-015.
