# FE-003 — Landing pública y búsqueda

## Objetivo

Landing pública, hero, buscador, chips, resultados y query params.

## Dependencias

- Backend relacionado: `BE-003`.
- QA relacionado: `QA-003`.
- Tokens y reglas visuales en `docs/opencode/references/frontend_visual_alignment.md`.

## Alcance MVP

- Implementar UI y consumo API del slice `003`.
- Mantener arquitectura Next.js/TypeScript/React/Tailwind.
- Reutilizar componentes desde `src/shared/ui`.
- Centralizar API en `src/shared/api`.
- Manejar loading, error, empty, success y submitting.

## Fuera de alcance

- App móvil nativa.
- Checkout en línea.
- Pasarela de pago de servicios.
- Productos, marketplace, carrito, inventario o facturación.
- Recomendaciones médicas automáticas.

## Actividades Frontend

1. Revisar contrato de `BE-003`.
2. Definir rutas en `src/app`.
3. Crear feature en `src/features`.
4. Crear componentes reutilizables si aplica.
5. Crear formularios con validación.
6. Integrar cliente API.
7. Manejar errores HTTP 400/401/403/404/409/422/500.
8. Agregar estados visuales.
9. Validar responsive.
10. Agregar pruebas si el repo tiene framework configurado.

## Criterios de aceptación

- La UI consume el contrato del slice o mock documentado.
- No hay Tailwind CDN.
- No hay links `#` en flujos implementados.
- No se muestran acciones que contradigan permisos conocidos.
- No se loggean datos sensibles.
- La interfaz es usable en desktop y mobile.

## Definition of Done

- [x] Rutas implementadas.
- [x] Componentes implementados.
- [x] Estados UX implementados.
- [x] Validaciones implementadas.
- [x] Consumo API centralizado.
- [x] Sin alcance fuera del MVP.

## Implementación

### Archivos creados/modificados

1. **Rutas**:
   - `src/app/page.tsx` — Landing pública con hero, how-it-works, CTA
   - `src/app/clinicas/page.tsx` — Listado de clínicas con búsqueda y filtros
   - `src/app/clinicas/[id]/page.tsx` — Detalle de clínica

2. **Componentes**:
   - `src/features/public-landing/components/HeroSection.tsx` — Hero con buscador
   - `src/features/public-landing/components/PublicSearchBar.tsx` — Barra de búsqueda
   - `src/features/public-landing/components/CategoryChips.tsx` — Chips de categorías
   - `src/features/public-landing/components/HowItWorksSection.tsx` — Sección instructiva
   - `src/features/public-landing/components/ProfessionalCtaSection.tsx` — CTA profesional
   - `src/features/public-landing/components/HeroBentoVisual.tsx` — Visual decorativo
   - `src/features/public-landing/components/CategoryChipsWrapper.tsx` — Wrapper con Suspense

3. **API**:
   - `src/shared/api/public.ts` — Cliente API para endpoints públicos

4. **Fixes técnicos**:
   - Wrapping de `useSearchParams()` en Suspense boundaries para evitar errores de prerendering
   - `export const dynamic = 'force-dynamic'` en páginas con query params
   - Build exitoso: 9/9 páginas generadas

### Resultados de gates

- **UIA-003**: 12/12 PASSED (Playwright UI automation)
- **UI Checks**: APPROVED (total 24/29 PASSED including previous slices)
- **Technical Checks**: ESLint PASS, TypeScript PASS, Next.js build PASS
