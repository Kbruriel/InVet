# FE-011 — Registro operativo de pagos de servicios

## Objetivo

Formulario pago operativo, recibo no fiscal, listados y filtros básicos.

## Dependencias

- Backend relacionado: `BE-011`.
- QA relacionado: `QA-011`.
- Tokens y reglas visuales en `docs/opencode/references/frontend_visual_alignment.md`.

## Alcance MVP

- Implementar UI y consumo API del slice `011`.
- Mantener arquitectura Next.js/TypeScript/React/Tailwind.
- Reutilizar componentes desde `src/shared/ui`.
- Centralizar API en `src/shared/api`.
- Manejar loading, error, empty, success y submitting.

## Fuera de alcance

- App móvil nativa.
- Checkout en línea.
- Pasarela de pago de servicios.
- Productos, marketplace, carrito, inventario o facturación.
- Recomendaciones médicas automáticas.

## Actividades Frontend

1. Revisar contrato de `BE-011`.
2. Definir rutas en `src/app`.
3. Crear feature en `src/features`.
4. Crear componentes reutilizables si aplica.
5. Crear formularios con validación.
6. Integrar cliente API.
7. Manejar errores HTTP 400/401/403/404/409/422/500.
8. Agregar estados visuales.
9. Validar responsive.
10. Agregar pruebas si el repo tiene framework configurado.

## Criterios de aceptación

- La UI consume el contrato del slice o mock documentado.
- No hay Tailwind CDN.
- No hay links `#` en flujos implementados.
- No se muestran acciones que contradigan permisos conocidos.
- No se loggean datos sensibles.
- La interfaz es usable en desktop y mobile.

## Definition of Done

- [x] Rutas implementadas. — `src/app/clinic/payments/` (registro, listado, detalle/recibo no fiscal)
- [x] Componentes implementados. — formulario de pago, listado por periodo, detalle con cancelación
- [x] Estados UX implementados. — loading/submitting/empty/success/error reutilizando `src/shared/ui`
- [x] Validaciones implementadas. — cambio >= 0 solo para efectivo, cita y servicio requeridos, mensaje claro por 403/404/409/422
- [x] Consumo API centralizado. — `src/shared/api/paymentClient.ts` (registro, listado, detalle, cancelación)
- [x] Sin alcance fuera del MVP. — sin checkout, sin pasarela, sin facturación

## Evidencia de cierre

- Frontend checks: `lint` PASS · `typecheck` PASS · `build` PASS · `jest` PASSED (`PaymentClient.test.ts`)
- UIA-011: `fe-011-payments.spec.ts` C1–C9 → 36/36 (chromium, firefox, webkit, mobile-chromium)
- QA-011: `docs/opencode/qa/QA-011-results.md` → Decision: `APPROVED`
