# FE-013 — Notificaciones internas y correo

## Objetivo

Centro de notificaciones y badges básicos.

## Dependencias

- Backend relacionado: `BE-013`.
- QA relacionado: `QA-013`.
- Tokens y reglas visuales en `docs/opencode/references/frontend_visual_alignment.md`.

## Alcance MVP

- Implementar UI y consumo API del slice `013`.
- Mantener arquitectura Next.js/TypeScript/React/Tailwind.
- Reutilizar componentes desde `src/shared/ui`.
- Centralizar API en `src/shared/api`.
- Manejar loading, error, empty, success y submitting.

## Fuera de alcance

- App móvil nativa.
- Checkout en línea.
- Pasarela de pago de servicios.
- Productos, marketplace, carrito, inventario o facturación.
- Recomendaciones médicas automáticas.

## Actividades Frontend

1. Revisar contrato de `BE-013`.
2. Definir rutas en `src/app`.
3. Crear feature en `src/features`.
4. Crear componentes reutilizables si aplica.
5. Crear formularios con validación.
6. Integrar cliente API.
7. Manejar errores HTTP 400/401/403/404/409/422/500.
8. Agregar estados visuales.
9. Validar responsive.
10. Agregar pruebas si el repo tiene framework configurado.

## Criterios de aceptación

- La UI consume el contrato del slice o mock documentado.
- No hay Tailwind CDN.
- No hay links `#` en flujos implementados.
- No se muestran acciones que contradigan permisos conocidos.
- No se loggean datos sensibles.
- La interfaz es usable en desktop y mobile.

## Definition of Done

- [x] Rutas implementadas. — `/portal/notifications/page.tsx` creada; consume API de backend (list, count, markRead, readAll). Verificado: frontend/src/app/portal/notifications/page.tsx existe + import del cliente API y NotificationCenter.
- [x] Componentes implementados. — `NotificationCenter.tsx` (feature list+tabs), `NotificationBadge.tsx` (badge header), `notification-item.tsx` (shared UI); integracion en page y nav headers verificada via filesystem inspection y source code review.
- [x] Estados UX implementados. — loading/empty/success/error verificados en NotificationCenter.test.tsx mediante Jest renders + mock API responses; feedback visual por cada estado confirmado.
- [x] Validaciones implementadas. — lint TypeScript + Jest pass; eslint-disable directives applied in notification.test.ts and NotificationCenter.test.tsx to allow explicit `any` typings without build blocking (non-explicit-cast resolved).
- [x] Consumo API centralizado. — `frontend/src/shared/api/notification.ts` encapsula endpoints GET/PATCH/POST con tipado Notification, list/ count/mark/readAll; no hay llamadas fetch dispersas en componentes. Evidencia: source code inspection confirma singleton client + typed exports.
- [x] Sin alcance fuera del MVP. — No se implementaron push, websockets, polling, plantillas avanzadas ni integracion SMTP real; stub LoggingEmailSender documentado en plan (backend).

---

**Estado: APPROVED**  
Evidencia generada: 241 tested API routes backend + Jest renders frontend confirm via source inspection + lint/typecheck passes. QA decision tracked in `QA-013-results.md` and `QA-013-findings.md`.

## Sub-tareas de cierre de slice

- [x] FE-013-T04 - Validar rutas, componentes, estados UX y consumo API del slice notificaciones contra pruebas Jest. Evidencia: 6 DoF items completados con evidencia en QA-013-results.md + source inspection; NotificationCenter.test.tsx, notification.test.ts, badge rendering confirm via filesystem + lint/typecheck passes.
