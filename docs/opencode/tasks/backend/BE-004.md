# BE-004 - Public clinic/branch profile

## Objective

Expose a public clinic/branch profile with services, schedules, rating summary, and basic availability.

## MVP scope

- Public endpoints under `/api/v1`.
- Include:
  - Available schedules
  - Offered services
  - Rating summary
  - Basic availability
- Do not include sensitive data, checkout, cart, or payment flows.

## Code layout

```text
app/
├── api/
│   ├── clinic_router.py
│   └── dependencies.py
├── application/
│   └── use_cases/
│       └── clinic_use_case.py
├── domain/
│   ├── entities/
│   │   └── clinic.py
│   └── repositories/
│       └── clinic_repository.py
└── infrastructure/
    ├── models/
    │   └── clinic_models.py
    └── repositories/
        └── clinic_repository_impl.py
```

## Endpoints

### Public branch profile

`GET /api/v1/clinics/branches/{branch_id}`

- No authentication required.
- Returns public branch data, services, schedules, and rating summary.

### Protected branch profile

`GET /api/v1/clinics/{clinic_id}/{branch_id}`

- Requires authentication.
- Returns the same payload with access validation for the authenticated user.

## Implementation Changes (BE-004)

### Architecture Improvements
- [x] Implemented dependency injection in `app/api/dependencies.py`
- [x] Routers now free of business logic  
- [x] Clean separation of API, Application, Domain and Infrastructure layers

### Security Enhancements  
- [x] Enhanced `is_branch_accessible()` method with proper validation
- [x] Public and protected endpoints properly separated
- [x] No ORM models exposed in API responses

## Acceptance notes

- Routers stay free of business logic.
- No ORM models are exposed in responses.
- Errors stay consistent and do not leak internal details.
- Tests cover happy path and negative path.

## Security Notes (Critical Issues)
⚠️ **Security issues remain pending resolution:**
1. Secret key configuration needs tightening (current key is development only)
2. Authentication token validation needs strengthening
3. Access control verification requires improvement  

These issues prevent full production approval but do not affect the core functionality which remains functional and meets MVP requirements.

(End of file - total 68 lines)