# BE-009 — Consulta médica básica

## Objetivo

Registro de consulta asociada a cita completada y mascota.

## Dependencias

- BE-008, FE-008, QA-008 cerrados.
- Revisar matriz en `docs/opencode/02_be_fe_qa_task_matrix.md`.
- Frontend relacionado: `FE-009`.
- QA relacionado: `QA-009`.

## Alcance MVP

- Implementar únicamente capacidades necesarias para el slice `009`.
- Exponer contratos bajo `/api/v1`.
- Mantener separación API/Application/Domain/Infrastructure/Core.
- Agregar pruebas automatizadas aplicables.

## Fuera de alcance

- Productos, marketplace, carrito, checkout, pasarela de pago de servicios, facturación electrónica y timbrado fiscal.
- Automatizaciones avanzadas o analítica avanzada salvo que este slice sea de hardening y solo como preparación documental.

## Actividades Backend

1. Revisar entidades y reglas existentes.
2. Crear o ajustar entidades/value objects de dominio.
3. Crear casos de uso en application.
4. Crear interfaces de repositorio/ports.
5. Implementar repositorios SQLAlchemy en infrastructure.
6. Crear schemas Pydantic separados por contexto.
7. Crear/ajustar routers FastAPI.
8. Crear migraciones Alembic si aplica.
9. Agregar pruebas Pytest/HTTPX.
10. Validar permisos, ownership e IDOR/BOLA cuando aplique.

## Criterios de aceptación

- El contrato requerido por `FE-009` existe o queda documentado como mock temporal aprobado.
- Los routers no contienen lógica de negocio.
- No se exponen modelos ORM.
- Las respuestas son paginadas cuando hay listados.
- Los errores son consistentes y no filtran información interna.
- Las pruebas cubren al menos happy path y un negative path.

## Definition of Done

- [x] Código backend implementado. Evidencia: `backend/app/{domain,infrastructure,application,api}` de consultas + `pytest app/tests/test_consultation_use_cases.py` (9 passed) y `pytest app/tests/api/test_consultations_api.py` (14 passed).
- [x] Migraciones aplicadas o justificadas como no requeridas. Evidencia: `alembic/versions/a009_consultations.py` (nullable `updated_at` + `server_default=CURRENT_TIMESTAMP` alineado con ORM).
- [x] Tests backend agregados/actualizados. Evidencia: `app/tests/test_consultation_use_cases.py` + `app/tests/api/test_consultations_api.py` (con override del nuevo port `get_by_user_id`).
- [x] Permisos y ownership validados. Evidencia: `_require_write_role`, `_resolve_created_by` vía `InternalUserRepository.get_by_user_id`; IDOR tests PASS.
- [x] OpenAPI consistente. Evidencia: `ConsultationRead`/`ConsultationPage` expuestos por router.
- [x] Sin alcance fuera del MVP. Evidencia: solo POST + GET detalle + GET listado.
