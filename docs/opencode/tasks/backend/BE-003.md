# BE-003 — Landing pública y búsqueda

## Objetivo

Endpoints públicos de clínicas, servicios, sucursales, filtros y paginación.

## Dependencias

- BE-002, FE-002, QA-002 cerrados.
- Revisar matriz en `docs/opencode/02_be_fe_qa_task_matrix.md`.
- Frontend relacionado: `FE-003`.
- QA relacionado: `QA-003`.

## Alcance MVP

- Implementar únicamente capacidades necesarias para el slice `003`.
- Exponer contratos bajo `/api/v1`.
- Mantener separación API/Application/Domain/Infrastructure/Core.
- Agregar pruebas automatizadas aplicables.

## Fuera de alcance

- Productos, marketplace, carrito, checkout, pasarela de pago de servicios, facturación electrónica y timbrado fiscal.
- Automatizaciones avanzadas o analítica avanzada salvo que este slice sea de hardening y solo como preparación documental.

## Actividades Backend

1. Revisar entidades y reglas existentes.
2. Crear o ajustar entidades/value objects de dominio.
3. Crear casos de uso en application.
4. Crear interfaces de repositorio/ports.
5. Implementar repositorios SQLAlchemy en infrastructure.
6. Crear schemas Pydantic separados por contexto.
7. Crear/ajustar routers FastAPI.
8. Crear migraciones Alembic si aplica.
9. Agregar pruebas Pytest/HTTPX.
10. Validar permisos, ownership e IDOR/BOLA cuando aplique.

## Criterios de aceptación

- El contrato requerido por `FE-003` existe o queda documentado como mock temporal aprobado.
- Los routers no contienen lógica de negocio.
- No se exponen modelos ORM.
- Las respuestas son paginadas cuando hay listados.
- Los errores son consistentes y no filtran información interna.
- Las pruebas cubren al menos happy path y un negative path.

## Definition of Done

- [x] Código backend implementado.
- [x] Migraciones aplicadas o justificadas como no requeridas.
- [x] Tests backend agregados/actualizados (68 tests passing).
- [x] Permisos y ownership validados.
- [x] OpenAPI consistente.
- [x] Sin alcance fuera del MVP.

## Implementación

### Archivos creados/modificados

1. **Routers públicos**:
   - `app/api/v1/routers/public_clinics.py` — Listado y detalle de clínicas públicas (`/api/v1/clinicas`)
   - `app/api/v1/routers/public_branches.py` — Listado de sucursales públicas con paginación (`/api/v1/sucursales`)
   - `app/api/v1/routers/public_services.py` — Listado de servicios públicos con filtros (`/api/v1/servicios`)

2. **Casos de uso**:
   - `app/application/use_cases/public_clinics.py` — `ListPublicClinicsUseCase`
   - `app/application/use_cases/public_branches.py` — `ListPublicBranchesUseCase`
   - `app/application/use_cases/public_services.py` — `ListPublicServicesUseCase`

3. **Repositorios**:
   - `app/infrastructure/database/repositories/branch_repository.py` — Interfaz `BranchRepository` con métodos públicos
   - `app/infrastructure/database/repositories/service_repository_impl.py` — Implementación `ServiceRepositoryImpl.list_public_services()`

4. **Schemas Pydantic**:
   - `app/api/v1/schemas/public_clinic.py` — DTOs públicos para clínicas
   - `app/api/v1/schemas/public_branch.py` — DTOs públicos para sucursales
   - `app/api/v1/schemas/public_service.py` — DTOs públicos para servicios

5. **Tests**:
   - `app/tests/api/test_public_clinics.py` — 6 contract tests (async)
   - `app/tests/api/test_public_branches.py` — 4 contract tests (async)
   - `app/tests/api/test_public_services.py` — 5 contract tests (async)

### Resultados de gates

- **QA-003**: APPROVED (13/13 criteria PASS, 28 tests passing)
- **Functional Review**: APPROVED
- **Clean Architecture Review**: APPROVED (re-revision after fixes)
- **Security Review**: APPROVED (all findings RESOLVED or ACCEPTED_RISK)
- **UI Checks**: APPROVED (UIA-003: 12/12 PASSED, total 24/29 PASSED)
- **Technical Checks**: APPROVED (68/68 tests, ruff/black/mypy clean, frontend build OK)

### Notas técnicas

- Los contract tests usan `httpx.AsyncClient` con `ASGITransport` porque los endpoints son async y `TestClient` hace llamadas síncronas.
- Las dependencias se mockéen sobre las funciones del router (`get_public_clinic_list_use_case`, etc.) no sobre las clases.
- Se aplicaron fixes de ruff: imports no usados, ordenamiento de imports, actualización de `collections.abc.Callable`.
