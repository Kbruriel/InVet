# BE-015 — Reportes operativos básicos

## Objetivo

Reportes de citas, servicios, mascotas, consultas, ratings y pagos por periodo.

## Dependencias

- BE-014, FE-014, QA-014 cerrados.
- Revisar matriz en `docs/opencode/02_be_fe_qa_task_matrix.md`.
- Frontend relacionado: `FE-015`.
- QA relacionado: `QA-015`.

## Alcance MVP

- Implementar únicamente capacidades necesarias para el slice `015`.
- Exponer contratos bajo `/api/v1`.
- Mantener separación API/Application/Domain/Infrastructure/Core.
- Agregar pruebas automatizadas aplicables.

## Fuera de alcance

- Productos, marketplace, carrito, checkout, pasarela de pago de servicios, facturación electrónica y timbrado fiscal.
- Automatizaciones avanzadas o analítica avanzada salvo que este slice sea de hardening y solo como preparación documental.

## Actividades Backend

1. Revisar entidades y reglas existentes.
2. Crear o ajustar entidades/value objects de dominio.
3. Crear casos de uso en application.
4. Crear interfaces de repositorio/ports.
5. Implementar repositorios SQLAlchemy en infrastructure.
6. Crear schemas Pydantic separados por contexto.
7. Crear/ajustar routers FastAPI.
8. Crear migraciones Alembic si aplica.
9. Agregar pruebas Pytest/HTTPX.
10. Validar permisos, ownership e IDOR/BOLA cuando aplique.

## Criterios de aceptación

- El contrato requerido por `FE-015` existe o queda documentado como mock temporal aprobado.
- Los routers no contienen lógica de negocio.
- No se exponen modelos ORM.
- Las respuestas son paginadas cuando hay listados.
- Los errores son consistentes y no filtran información interna.
- Las pruebas cubren al menos happy path y un negative path.

## Definition of Done

- [x] Código backend implementado. — 5 use-cases de agregación en `backend/app/application/usecases/reports/` (report_appointments, report_services, report_pets_count, report_consultations, report_payments, report_ratings_summary) + 6 endpoints `reports_router.py` registrados en `api/v1/router.py` (prefix `/reports`). Checkpoint `BE-015-backend.json`: T01–T10 `completed`, `outcome: pass`.
- [x] Migraciones aplicadas o justificadas como no requeridas. — BE-015 es agregación read-only: **sin nueva migración** (checkpoint `runtime_gate.alembic.be015_new_migrations: 0`; head `a013`; `a014` corresponde a BE-014).
- [x] Tests backend agregados/actualizados. — 12 ficheros `test_reports_*.py` (7 use-cases + 5 integración). Suite suite: `62 passed` (T08 12 + T09 24 + T10 38); suite global `630 passed, 1 skipped` (BE-015-checks.md).
- [x] Permisos y ownership validados. — `test_reports_auth.py` + `test_reports_tenant_isolation.py` (12/12 + 12/12): 401 sin token / inválido / expirado, `clinic_id` derivado del JWT (no expuesto como query param → BOLA-safe), tenant A/B aislados, faked pets no expanden.
- [x] OpenAPI consistente. — 6 rutas reportes presentes en la spec (total_paths 68): `/api/v1/reports/{appointments,consultations,payments,pets,ratings,services}` (checkpoint `runtime_gate.openapi.report_paths`).
- [x] Sin alcance fuera del MVP. — solo lectura de reportes agregados; sin export, sin gráficos, sin permisos extra. `BE-015-checks.md` `Decision: APPROVED`.

## Evidencia de cierre (2026-09-08)

- `python backend/scripts/validate_slice_plan.py BE-015 --stage docs` → **`[PASS] BE-015/FE-015/QA-015 stage=docs`**.
- `docker compose ps`: `invet-db`, `invet-backend`, `invet-frontend` → **Up (healthy)** (rebuild `--build --force-recreate`).
- `backend/app/tests` → **630 passed, 1 skipped** (tras fix de reports_pets_dto + notification_repo.unread + payload sync).
- `QA-015` → **APPROVED**; 129/129 tests BE-015 verdes; 13 fallos full-suite `ACCEPTED_RISK` (QF-015-03, ajenos a BE-015).
