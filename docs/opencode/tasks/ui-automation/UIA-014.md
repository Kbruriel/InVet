---
schema_version: 3
slice: "014"
canonical_plan: BE-014
status: PLANNED
encoding: UTF-8
---

# BE-014 Plan — Soporte básico

## Objetivo del slice

Registrar solicitudes de soporte, categorias, estados y consulta por usuario/admin.

## Brief operativo del slice

| Campo | Valor |
| --- | --- |
| Titulo | Soporte básico |
| Descripcion | Registrar solicitudes de soporte (tickets), categorías disponibles y estados transicionables; permitir que usuarios creen tickets personales, consulten sus propios tickets, y admin gestione tickets de la clinica. |
| Entregables backend | Modelos Support + Category + enum Status; migracion a014; repositorio; use cases para crear/listar/detail/update-status; endpoints CRUD protegidos con auth/ownership. |
| Entregables frontend | Formulario de soporte (page en `/portal/support/new`), listado de tickets del usuario (`/portal/support`) y detalle (`/portal/support/[id]`); si admin => `/admin/support`. |
| Criterios QA principales | Ticket se registra y consulta; estados consistentes; solo owner/admin accede; cross-tenant falla seguro (404) sin enumeration. |

Fuente obligatoria: `docs/opencode/references/slice_task_context.md` (fila 014).

## Alcance MVP

- Entidad principal **Ticket** con campos: id, title, description(priority), status(iniciado/pendiente/proceso/completado/cerrado), category(id/titulo), user_id(intern_users para admin/internal o owners para propietario), clinic_id, created_at, updated_at.
- Entidad **Category** como tabla de referencias: id, name(unico por clinica), description, active(true).
- Endpoints CRUD basicos protegidos con JWT bearer tokens.
- Listados por owner/usuario autenticado (con tenant isolation por clinic_id).
- Cambio de estado solo por admin interno (con guardas de ownership).
- Paginacion en listados del backend (page, page_size 5-100).

## Fuera de alcance

- Asignacion de tickets a veterinarios especificos (Stage 2).
- Notificaciones push/internas linked a ticket (se deja abierto para BE-013/BE-017).
- Reportes de SLA o tiempos de respuesta.
- Integracion con email externo al stub LoggingEmailSender.
- Busqueda full-text en contenido de tickets.

## Suposiciones

| Supuesto | Implicacion | Decision |
| --- | --- | --- |
| Admin sistema BE-016 aun no implementado | Para este slice, admin se mapea a InternalUser/Owner con permiso "admin" del token. | No cambia contract publico. |
| Propietario y usuario interno comparten tenant (clinic_id) | Ownership por owner_id para propietario; clinic_id para internal_user. | Consistente con AC QA-014 acceso cruzado. |

## Revision de gaps

| Fuente revisada | Gap | Decision | Impacto en tareas |
| --- | --- | --- | --- |
| slice_task_context.md (fila 014) | Sin detalles de tabla Status vs Category en brief | Extraidos de AC-014-02; status como enum in DB. | BE-014-T01 agrega enum a entidad Status. |
| FE-014.md | Formulario solo para propietario, no para admin crear tickets | Admin solo gestiona/modifica estado (no crea). | FE-014-T01 solo en portal; FE-014-T02 en admin panel (BE-016). |

## Entidades y reglas de negocio

| Entidad / regla | Fuente | Responsabilidad del slice | Validacion |
| --- | --- | --- | --- |
| Ticket (entity + ORM) | AC-014-01 | Modelo, campos y constraint unico por usuario+createdAt(dup prevention). | Pytest model validation + migration up/down. |
| Category (entity + ORM) | AC-014-02, AC-014-06 | Enum de status; tabla de category con active flag. | Pytest unique constraint + migration reversible. |
| Status enum | AC-014-03, AC-014-04 | Transiciones validas: iniciado→pendiente/proceso→completado/cerrado. | Pytest state transitions + pytest status guard in router. |

## Fuentes y artefactos de contexto

| Artefacto | Ruta | Uso por agentes | Estado |
| --- | --- | --- | --- |
| Matriz BE/FE/QA | `docs/opencode/02_be_fe_qa_task_matrix.md` | Alcance vertical y correspondencia de IDs | REQUIRED |
| Tarea backend | `docs/opencode/tasks/backend/BE-014.md` | Reglas, entidades, persistencia y API | REQUIRED |
| Tarea frontend | `docs/opencode/tasks/frontend/FE-014.md` | Rutas, UI, estados y contrato de cliente | REQUIRED |
| Tarea QA | `docs/opencode/tasks/qa/QA-014.md` | Criterios de aceptacion y riesgos | REQUIRED |
| User Stories | `docs/opencode/tasks/user-stories/US-014.md` | Historias US-014-NN, criterios AC-014-NN | REQUIRED |
| UI Automation | `docs/opencode/tasks/ui-automation/UIA-014.md` | Casos y cobertura Playwright | REQUIRED |
| API Automation | `docs/opencode/tasks/api-automation/APIA-014.md` | Cobertura HTTPX endpoints+seguridad | REQUIRED |
| Brief contexto | `docs/opencode/references/slice_task_context.md` | Titulo, descripcion, entregables y criterios por slice | REQUIRED |
| Referencia Spec Kit | `docs/opencode/references/spec_kit_reference_improvements.md` | Principios de tareas pequenas y trazabilidad | RECOMENDED |
| Template schema v3 | `docs/opencode/templates/slice_plan_template.md` | Formato canonico del plan | REQUIRED |

## Matriz de trazabilidad

| ID | Fuente | Historia o criterio | Tarea planificada | Validacion | Evidencia esperada | Estado |
| --- | --- | --- | --- | --- | --- | --- |
| AC-014-01 | US-014 / BE-014 | POST /api/v1/tickets crea ticket nuevo con validacion de title obligatoria, status=inicio y category opciona. | BE-014-T05 (endpoint create) + BE-014-T03+T04 (entity/repo/usecase); APIA-014 C1,C4 | POST 201 con payload completo; duplicate prevention by user_id; pytest happy path PASS | Tabla tickets con fila insertada; status=inicio category_id=NULL; title unique per user per day. |
| AC-014-02 | US-014 / BE-014 | Tabla categories con seed minimo (3 items) y constraint name unico por clinica active. | BE-014-T07 (seed script); migration a014 categories + unique | Seed 3 rows; unique on (clinic_id,name) enforced; alembic up/down works | 3 filas en categories; UNIQUE uq_ticket_category active index presente. |
| AC-014-03 | US-014 / BE-014 | GET /api/v1/tickets?page=1&page_size=20 con filtros por user_id/clinic_id/status listados paginados. | BE-014-T05 (endpoint list); repo list_by_user; APIA-014 C3,C11 | Paginated 200 items with meta; filters applied; page sizes validated | Listado correcto con meta {page,total,pages}; filtro status funciona; pagination limits applied. |
| AC-014-04 | US-014 / BE-014 | GET /api/v1/tickets/{id} obtiene detalle solo propietario/admin; 404 si ajeno a tenant/owner. | BE-014-T05 (endpoint detail); guardas ownership; APIA-014 C4,C13 | Owner gets detail; cross-tenant returns 404 without revealing existence | Detail payload valid (id,title,status,category,customer info); 404 for cross-tenant. |
| AC-014-05 | US-014 / BE-014 | PATCH /api/v1/tickets/{id}/status cambia estado con reglas de transicion; solo admin internal_user puede cambiar. | BE-014-T05 (endpoint update-status); state machine logic; APIA-014 C4,C8 | Status transitions validated; admin-only guard works | Transition from 2→3 or 3→4; 403 for non-admin; 422 for invalid transition. |
| AC-014-06 | US-014 / BE-014 | GET /api/v1/tickets/{id}/category obtiene categoria asignada o null sin error si no existe. | APIA-014 C12,C6; endpoint detail includes category or null | Category returns with ticket details (if exists) or null if none | Consistent category field in GET ticket response. |
| AC-014-07 | US-014 / BE-013 / FE-014 | Sin bearer → 401 en todos los endpoints privados de tickets. | BE-014-T05 (guards) + QA-014 minimal auth; APIA-014 C9 | All endpoints require valid Bearer; 401 on missing token | Consistent 401 across all five ticket endpoints. |
| AC-014-08 | US-014 / FE-014 | UI formulario de soporte con campos: titulo(texto), descripcion(textarea), categoria(select) y enviar; validacion en cliente antes del POST. | FE-014-T01 + UIA-014 C2,C3,C5; QA-014 minimal form states | Fields render correctly; client validation blocks empty title; submit shows toast success/invalid fields. | Form renders with 200px min height textarea; select populated from categories API; validation shown on submission attempt without title. |
| AC-014-09 | US-014 / FE-014 | Listado de tickets del usuario autenticado con filtro por estado, paginacion visual y EmptyState sin datos. | FE-014-T02 + UIA-014 C7; QA-014 states | Listado muestra tickets correctos; filtrado funciona por status; pagination visible; empty state shown when no data. | Tickets listed with title, status, created; filter dropdown works; page controls visible; no-data state renders correctly. |
| AC-014-10 | US-014 / BE-014 | Migracion a014 reversible: tablas support_ticket + ticket_category (name unique) con FK cascada. | BE-014-T06 (migration); QA-014 minimal migration check | alembic up/down/up works; tables and constraints properly set on revert | 3 commands (upgrade head/downgrade/-1/upgrade head) all successful with no foreign key or unique constraint errors. |
| AC-014-11 | US-014 / BE-014 | IDOR/BOLA: usuario de clinica B no puede crear, listar ni acceder a tickets de clinica A. | BE-014-T05 (guards) + QA-014 minimal; APIA C4,C13 | Cross-clinic GET/POST returns 401 or lists empty/no rows accessible; creation uses user's clinic | No data leak from cross-tenant access; create uses authed user's tenant. |

## Endpoints esperados

| Accion | Metodo | Ruta `/api/v1` | Auth | Request | Response | Errores |
| --- | --- | --- | --- | --- | --- | --- |
| Crear ticket | POST | `/api/v1/tickets` | Bearer con tenant_id (owner o internal) | `{title, description, category_id?}` | 201 `TicketRead` created with status=iniciado, user/clinic mapped | 400 title required; 403 invalid role/tenant; 422 validation; 500 unexpected |
| Listar mis tickets | GET | `/api/v1/tickets?page=1&page_size=20&status=pendiente` | Bearer con tenant_id (owner solo sus own) | Query: page int>=1, page_size int 5-100, status? enum | 200 `{items: [TicketListItem], meta: {page,pagesizes,total,pages}}` | 401 sin token; 422 invalid params; 500 unexpected |
| Obtener detalle | GET | `/api/v1/tickets/{id}` | Bearer propietario o admin de clinica del ticket | Path: id int | 200 `TicketRead` completo (incluyendo category nombre, customer) | 401; 404 si ID no existe o ajeno al tenant/owner; 500 unexpected |
| Actualizar status | PATCH | `/api/v1/tickets/{id}/status` | Bearer internal_user de clinica del ticket (solo admin/system roles) | `{new_status: "completado"|"cerrado"}` | 200 `TicketStatusUpdate {ticket_id, old_status, new_status}` | 401; 403 no admin; 422 invalid transition; 404 si ID not found or tenant mismatch; 500 unexpected |
| Listar categorias | GET | `/api/v1/tickets/categories` | Bearer activo (sin filtrado de tenant) | none | 200 `{items: [{id, name, active}]}` | 401 sin token; 500 unexpected |

Schemas: `TicketCreate`, `TicketRead`, `TicketListItem`, `TicketStatusUpdate`, `CategoryItem`. Enum ticket_status: `iniciado|pendiente|proceso|completado|cerrado`.

## Contrato de implementacion frontend

### Rutas y acceso

| Ruta | Componente | Rol requerido | Fuente |
| --- | --- | --- | --- |
| `/portal/support` (Listado) | SupportPage con filtro por estado. Propietarios autentificados. | US-014-02, FE-014-T02 |
| `/portal/support/new` (Formulario) | SupportForm page para crear un ticket nuevo. Propietarios autentificados. | US-014-01, FE-014-T01 |
| `/portal/support/[id]` (Detalle) | TicketDetail page mostrando toda la informacion del ticket y cambio de estado visible solo si el usuario tiene permiso admin interno. | AC-014-04 |

### Flujos y estados UX

1. **Formulario**: Usuario propietario navega a `/portal/support/new` → ve formulario con campos obligatorio: titulo (max 200 chars), descripcion (textarea, max 2000 chars, opcionales en brief pero obligatorios segun validation logic), categoria (select populated from GET categories). Al enviar → POST to `/tickets` → muestra toast de exito o error (sin internals).
2. **Listado**: Usuario navega a `/portal/support` → lista paginada de sus tickets con columns: titulo, estado, fecha creacion, acciones(ver detalle). Filtro por status dropdown arriba del listado.
3. **Detalle**: Click en un ticket → muestra info completa (id, titulo, descripcion, estado, categoria, creado por, fecha). Boton cambiar estado solo aparece si usuario es admin interno de la clinica del ticket.
4. **Estado vacío**: si el usuario no tiene ningun ticket → `EmptyState` centrado ("Sin solicitudes de soporte registradas").
5. **Error banner**: HTTP 4xx/5xx muestra `<ErrorBanner>` en zona superior; sin internals expuestos.

Estados UX: loading, empty, success, error, submitting.

### Contratos API por accion (frontend)

| Accion UI | Endpoint | Metodo | Request | Response | Errores | Auth |
| --- | --- | --- | --- | --- | --- | --- |
| Crear ticket | `/api/v1/tickets` | POST | `{title: string, description?: string, category_id?: int}` | 201 `TicketRead` | 400 missing title; 403 tenant; 422 invalid form; 500 unexpected | Bearer activo tenant_id |
| Listar mis tickets | `/api/v1/tickets?page=1&page_size=20&status=` | GET | Query params: page>=1, pageSize 5-100, status?(optional) | 200 `{items:[TicketListItem],meta:{page,pages,total}}` | 401 sin token; 422 invalid params; 500 unexpected | Bearer activo tenant_id |
| Ver detalle ticket | `/api/v1/tickets/[id]` | GET | none | 200 `TicketRead` completo | 401 sin token; 404 no existe o ajeno tenant; 500 unexpected | Bearer propietario o admin |
| Cambio de estado | `/api/v1/tickets/[id]/status` | PATCH | `{new_status: enum}` | 200 `TicketStatusUpdate` | 401; 403 no admin; 422 invalid transition; 404 ajeno tenant; 500 unexpected | Bearer internal_user admin de clinica |
| Obtener categorias | `/api/v1/tickets/categories` | GET | none | 200 `{items:[CategoryItem]}` | 401 sin token; 500 unexpected | Bearer activo |

### Formularios y validacion

- Campo **titulo**: texto obligatorio, max 200 chars. Mostrar "El titulo es obligatorio" en 32px min height label con border if validation fails on blur/submit.
- Campo **descripcion**: opcionales (min 10 chars si proporcionado), max 2000.
- Categoria: select populated from API; sin category → null persisted as NULL in DB for this slice (future BE allows required category).
- Client-side validation: title must be at least 5 characters to match backend min length; description if present starts with minimum 10 chars. Error message shown inline on blur or form submit attempt.

### Arquitectura de componentes

| Componente | Ubicacion | Responsable | Propósito |
| --- | --- | --- | --- |
| `SupportForm.tsx` | `frontend/src/features/support/` | Formulario crear ticket con validacion cliente-side. |
    _support_