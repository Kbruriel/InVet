# 11 - ChatGPT Project Context

## Purpose

This document is a compact source of truth for using InVet inside ChatGPT or similar assistants.
It explains the operating model, agent structure, commands, workflow, and expected outputs.

## Project summary

InVet uses a sequential, slice-based operating model.
Each slice is vertical and must stay aligned across user stories, backend, frontend, QA, UI automation, and API automation:

```text
US-00X + BE-00X + FE-00X + QA-00X + UIA-00X + APIA-00X = one verifiable functional slice
```

The project is intentionally constrained to MVP scope.
Do not introduce products, marketplace, cart, checkout, service payment gateway, electronic invoicing, or fiscal stamping into MVP.

## Core operating model

Work is organized through OpenCode agents and slash commands.
The standard lifecycle for a slice is:

```text
/execute-slice BE-00X
```

Manual flow:

```text
/plan-task BE-00X|FE-00X|QA-00X
/implement-backend-task BE-00X
/implement-frontend-task FE-00X
/implement-ui-automation-task FE-00X
/implement-api-automation-task BE-00X
/qa-task QA-00X
/review-slice BE-00X
/implement-findings BE-00X
/qa-task QA-00X
/clean-architecture-review BE-00X
/security-review BE-00X
/run-ui-checks FE-00X
/run-checks BE-00X
/update-docs BE-00X
/final-gate BE-00X
```

General rules:
- Planning accepts BE, FE, or QA IDs and normalizes the same slice index.
- Keep backend, frontend, and QA aligned by index.
- Backend defines contracts first.
- Frontend consumes the contract of the same slice.
- QA validates the full slice.
- All operational agents can use `docker compose` when the slice requires PostgreSQL or container runtime validation. Use `db` + `backend` for data-backed tests and `frontend` for UI/runtime validation when applicable.
- Reviews, checks, and documentation must be closed before advancing.
- Use `invet-command-executor` for mechanical command batches, test runs, log collection, and repeat verifications.
- `invet-command-executor` and its fallback inherit the model selected by the user; no executor pins a default model.
- Reserve the fallback for checks and logs, failed-command retries, and mechanical fixes that need an isolated retry path.
- Use `invet-final-reviewer` for the final release gate after QA, reviews, checks, and docs when a higher-capacity second opinion is needed.

Skill usage rule:
- The available Codex skill is `customize-opencode`.
- `invet-backend-implementer`, `invet-frontend-implementer`, and the other `invet-*` names are OpenCode agents, not Codex skills.
- Invoke them through slash commands such as `/implement-backend-task BE-00X`, not as `Skill invet-backend-implementer`.

## Relevant repository structure

```text
.opencode/
  agents/
  commands/

docs/opencode/
  00_installation_manifest.md
  01_command_runbook.md
  02_be_fe_qa_task_matrix.md
  03_task_prompt_contracts.md
  04_agent_contracts.md
  05_done_gates_by_command.md
  plans/
  tasks/
    user-stories/
    backend/
    frontend/
    qa/
    ui-automation/
    api-automation/
  references/
    slice_task_context.md
  templates/
```

Important supporting files:
- `docs/opencode/02_be_fe_qa_task_matrix.md`: canonical matrix of slices.
- `docs/opencode/references/slice_task_context.md`: per-slice brief for title, description, BE/FE deliverables, and QA acceptance focus.
- `docs/opencode/plans/BE-00X-plan.md`: numbered implementation checklist generated by `/plan-task`.
- `docs/opencode/tasks/user-stories/US-00X.md`: slice user stories and numbered acceptance criteria.
- `docs/opencode/tasks/backend/BE-00X.md`: backend scope and contract.
- `docs/opencode/tasks/frontend/FE-00X.md`: frontend expectations for the same slice.
- `docs/opencode/tasks/qa/QA-00X.md`: QA validation for the same slice.
- `docs/opencode/tasks/ui-automation/UIA-00X.md`: UI automation coverage and evidence.
- `docs/opencode/tasks/api-automation/APIA-00X.md`: API automation coverage and evidence.

## Agent structure

### Primary orchestration

`invet-orchestrator`
- Mode: `primary`
- Objective: coordinate the complete slice flow and prevent skipping gates.

### Planning

`invet-product-planner`
- Mode: `subagent`
- Objective: translate a BE, FE, or QA ID into one schema v3 vertical plan in `docs/opencode/plans/BE-00X-plan.md`.
- Output: backend contract, detailed frontend contract, QA scope, traceability, Docker/test contract, report/finding plan, UTF-8 policy, dependencies, deliverables, validation, evidence, and measurable tasks.

### Implementation

`invet-backend-implementer`
- Mode: `all`
- Objective: implement pending backend tasks from the plan using FastAPI, SQLAlchemy 2.0, Alembic, Pytest, and Clean Architecture.
- Expected areas: entities, use cases, repositories, ORM, migrations, schemas, routers, tests, `/api/v1`.
- Marks plan tasks as complete only after acceptance criteria are verified.

`invet-frontend-implementer`
- Mode: `all`
- Objective: implement pending frontend tasks from the plan using Next.js, TypeScript, React, and Tailwind.
- Expected areas: routes, layouts, components, forms, validation, API client, UX states, tests.
- Marks plan tasks as complete only after acceptance criteria are verified.

`invet-ui-automation-implementer`
- Mode: `subagent`
- Objective: implement E2E UI coverage in `InVet_UI_Automation/tests/e2e`.
- Expected areas: navigation, forms, role visibility, redirects, protected routes, UI states, smoke and regression tags.

`invet-api-automation-implementer`
- Mode: `subagent`
- Objective: implement external API contract coverage in `InVet_UI_Automation/tests/api`.
- Expected areas: payloads, statuses, authn/authz, IDOR/BOLA, tenant isolation, sensitive data exposure.

### Validation and review

`invet-qa-validator`
- Mode: `all`
- Objective: validate acceptance, integration, contract, security and regression; reject missing product unit tests without repairing them.
- Expected artifact: QA evidence in Markdown with traceability to plan tasks, completed QA validation tasks marked in the plan, and a findings file when execution or configuration blocks occur.

`invet-slice-reviewer`
- Mode: `all`
- Objective: review plan plus implementation across BE, FE, and QA for the same slice.
- Use when: you need a targeted review before architecture/security global gates.
- Expected artifact: `docs/opencode/reviews/BE-00X-review.md` based on `docs/opencode/templates/review_findings_template.md`.

`invet-findings-implementer`
- Mode: `all`
- Objective: implement review findings, close corrections, and document the result.
- Expected artifacts: `docs/opencode/reviews/BE-00X-corrections.md` based on `docs/opencode/templates/corrections_checklist_template.md`, plus any slice review files that feed the corrections flow.

`invet-command-executor`
- Mode: `all`
- Objective: run command-heavy work, tests, lint, diffs, log collection, and repeat checks with the model selected by the user.
- Expected artifact: exact command output and any safe mechanical fix requested by the parent agent.

`invet-command-executor-fallback`
- Mode: `all`
- Objective: serve as a mechanical fallback when the primary executor is unavailable or an isolated retry path is needed.
- Expected artifact: exact command output and any safe mechanical fix requested by the parent agent.

`invet-clean-architecture-reviewer`
- Mode: `subagent`
- Objective: verify backend layering and frontend modular separation without modifying code.
- Expected artifact: `docs/opencode/reviews/BE-00X-clean-architecture-review.md` based on `docs/opencode/templates/review_findings_template.md`.

`invet-security-reviewer`
- Mode: `subagent`
- Objective: verify OWASP concerns, permissions, IDOR/BOLA, token handling, logging, and data exposure without modifying code.
- Expected artifact: `docs/opencode/reviews/BE-00X-security-review.md` based on `docs/opencode/templates/review_findings_template.md`.

### Operational support

`invet-check-runner`
- Mode: `all`
- Objective: run backend and frontend checks and summarize actionable failures.

`invet-docs-updater`
- Mode: `all`
- Objective: update Markdown documentation after the slice is implemented and validated.

`invet-final-reviewer`
- Mode: `subagent`
- Objective: perform the final release gate with a higher-capacity review after QA, reviews, checks, and docs.
- Expected artifact: `docs/opencode/reviews/BE-00X-final-review.md` based on `docs/opencode/templates/review_findings_template.md`.

## Command structure

`/plan-task BE-00X|FE-00X|QA-00X`
- Agent: `invet-product-planner`
- Produces `docs/opencode/plans/BE-00X-plan.md`.
- The plan uses schema v3 and includes layer, type, criterion, single responsibility, context, contracts, dependencies, deliverables, validation, expected outcome and evidence per task.
- No source code changes.

`/implement-backend-task BE-00X`
- Agent: `invet-backend-implementer`
- Implements only pending backend tasks from `docs/opencode/plans/BE-00X-plan.md`.
- Marks completed backend tasks as `- [x]` only after criteria are verified.

`/implement-frontend-task FE-00X`
- Agent: `invet-frontend-implementer`
- Implements only pending frontend tasks from `docs/opencode/plans/BE-00X-plan.md`.
- Marks completed frontend tasks as `- [x]` only after criteria are verified.

`/implement-ui-automation-task FE-00X`
- Agent: `invet-ui-automation-implementer`
- Implements UI automation specs for the slice and updates `UIA-00X.md`.

`/implement-api-automation-task BE-00X`
- Agent: `invet-api-automation-implementer`
- Implements API automation specs for the slice and updates `APIA-00X.md`.

`/qa-task QA-00X`
- Agent: `invet-qa-validator`
- Validates backend, frontend, and integration for the slice.
- Uses plan objectives and acceptance criteria to derive QA cases.
- Creates or adjusts QA-level tests and executes available tests.
- Rejects missing backend/frontend unit tests and delegates their fix.
- Marks completed QA validation tasks as `- [x]` only after evidence exists.
- Writes `docs/opencode/qa/QA-00X-findings.md` when environment or execution issues block QA.

`/review-slice BE-00X` o `/review-slice FE-00X`
- Agent: `invet-slice-reviewer`
- Reviews the plan and implementation of the slice.
- Creates a Markdown findings report if corrections are required.

`/implement-findings BE-00X` o `/implement-findings FE-00X`
- Agent: `invet-findings-implementer`
- Fixes the findings raised by the slice review for the same vertical slice.
- If invoked with `FE-00X`, it derives the matching `BE-00X` and keeps the same slice index.
- Creates a Markdown corrections report and checklist.

`/clean-architecture-review`
- Agent: `invet-clean-architecture-reviewer`
- Global architecture gate.
- Creates `docs/opencode/reviews/BE-00X-clean-architecture-review.md` when findings exist.

`/security-review`
- Agent: `invet-security-reviewer`
- Global security gate.
- Creates `docs/opencode/reviews/BE-00X-security-review.md` when findings exist.

`/run-checks`
- Agent: `invet-check-runner`
- Runs tests, lint, typecheck, and build when available.

`/run-ui-checks`
- Agent: `invet-check-runner`
- Runs `npm run test:e2e` and `npm run test:regression` from `InVet_UI_Automation`.

`/update-docs`
- Agent: `invet-docs-updater`
- Updates task state, risks, decisions, and related documentation.

## Workflow expectations

### Per-slice execution

1. Plan the slice from `BE-00X`.
2. Implement backend first.
3. Implement frontend against the same slice contract.
4. Implement UI automation and API automation for the same slice.
5. Execute QA for the same index.
6. Review the full slice and document findings if needed.
7. Implement findings and document closure.
8. Pass architecture review.
9. Pass security review.
10. Pass UI checks.
11. Run checks.
12. Update documentation.
13. Optionally run the final release gate.

### Gate expectations

After planning:
- MVP scope is clear.
- Out-of-scope is explicit.
- Backend and frontend contracts are defined.
- QA scope is identified.
- `docs/opencode/plans/BE-00X-plan.md` passes `validate_slice_plan.py --stage plan`.

After backend:
- API stays under `/api/v1`.
- Routers do not contain business logic.
- Domain is framework-independent.
- Repositories are abstracted.
- Tests exist when applicable.
- Backend plan tasks are marked complete only when acceptance criteria are verified.

After frontend:
- Routes and components are implemented.
- API consumption is centralized.
- UX states exist: loading, error, empty, success.
- Private routes are protected when applicable.
- Frontend plan tasks are marked complete only when acceptance criteria are verified.

After QA:
- QA cases are derived from plan objectives and acceptance criteria.
- Happy path validated.
- Negative path validated.
- Permissions and IDOR/BOLA validated.
- Regression minimally covered.
- Evidence recorded.
- Evidence is traced back to plan tasks.
- Test creation/execution documented.
- QA blockers documented in Markdown if the environment prevented execution.

Before moving to the next slice:
- Review findings are closed or explicitly accepted.
- Architecture review is approved.
- Security review is approved.
- Checks are green or failures are documented.
- Documentation is updated.
- If the final gate is used, it is approved and the closing evidence is consistent.

## Review and correction artifacts

Expected artifact locations:

```text
docs/opencode/plans/BE-00X-plan.md
docs/opencode/reviews/BE-00X-review.md
docs/opencode/reviews/BE-00X-clean-architecture-review.md
docs/opencode/reviews/BE-00X-security-review.md
docs/opencode/reviews/BE-00X-corrections.md
```

Templates:
- `docs/opencode/templates/review_findings_template.md`
- `docs/opencode/templates/corrections_checklist_template.md`
- `docs/opencode/templates/slice_result_template.md`

## Guidance for ChatGPT usage

When using this project as context in ChatGPT:
- Treat `docs/opencode/tasks/**` as the primary functional source for each slice.
- Preserve the same index across BE, FE, and QA.
- Do not expand scope beyond MVP unless explicitly requested.
- Prefer backend-first implementation.
- Always reason about permissions, data exposure, and IDOR/BOLA.
- Respect Clean Architecture boundaries.
- Document findings and corrections in Markdown rather than leaving them implicit.

## Recommended source files to attach or preload

- `docs/opencode/11_chatgpt_project_context.md`
- `docs/opencode/02_be_fe_qa_task_matrix.md`
- `docs/opencode/references/slice_task_context.md`
- `docs/opencode/03_task_prompt_contracts.md`
- `docs/opencode/04_agent_contracts.md`
- `docs/opencode/12_troubleshooting_skills_vs_agents.md`
- The specific slice files:
  - `docs/opencode/tasks/backend/BE-00X.md`
  - `docs/opencode/tasks/frontend/FE-00X.md`
  - `docs/opencode/tasks/qa/QA-00X.md`
