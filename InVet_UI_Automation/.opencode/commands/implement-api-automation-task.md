---
description: Implementa automatizacion API para un slice InVet.
agent: invet-api-automation-implementer
---

Implementa la automatizacion API para `$ARGUMENTS`.

Instrucciones:
1. Lee `US-00X`, `BE-00X`, `FE-00X`, `QA-00X` y `APIA-00X`.
2. Implementa specs en `tests/api/`.
3. Referencia `US-00X-NN` y `CA-NN`.
4. Ejecuta `npm run test:api` cuando el entorno este listo.
5. Documenta evidencia y bloqueos.

