---
description: Implementa automatizacion UI/E2E para un slice InVet.
agent: invet-ui-automation-implementer
---

Implementa la automatizacion UI para `$ARGUMENTS`.

Instrucciones:
1. Lee `US-00X`, `BE-00X`, `FE-00X`, `QA-00X` y `UIA-00X`.
2. Implementa specs en `tests/e2e/`.
3. Referencia `US-00X-NN` y `CA-NN` en titulos o anotaciones.
4. Ejecuta `npm run test:e2e` y `npm run test:regression` cuando el entorno este listo.
5. Documenta evidencia y bloqueos.

